server:
  port: 5000

spring:
  application:
    name: management-system-api-gateway
  config:
    import: configserver:http://localhost:8888
  main:
    banner-mode: off
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              # Enable auto created locator (Keep false if you have custom routes)
              enabled: false
              # Disable upper case name for services
              lower-case-service-id: true
          routes:
            - id: management-system-users-id
              uri: ${USERS_SERVICE_URL:lb://management-system-users}
              predicates:
                - Path=/api/users/**
                - Method=GET, POST
              filters:
                - RewritePath=/management-system-users/(?<segment>.*), /$\{segment}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoint:
    gateway:
      access: read_only
  endpoints:
    web:
      exposure:
        include: health,info,gateway

logging:
  level:
    org:
      springframework:
        cloud:
          gateway: DEBUG
      web:
        reactive: DEBUG